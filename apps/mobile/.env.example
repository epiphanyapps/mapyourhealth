# Google Maps API Key (OPTIONAL)
# Only needed if using native Google Maps SDK for map rendering.
# NOT required for address autocomplete - that's handled by the backend.
#
# If you need Maps SDK:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable "Maps SDK for iOS/Android"
# 4. Create an API key with appropriate restrictions
# 5. Copy the key below
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=

# NOTE: Google Places Autocomplete API is proxied through the backend.
# The API key for Places is stored server-side using Amplify Secrets.
# See packages/backend/amplify/functions/places-autocomplete for details.
